{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "regions": ["bom1"],
  "functions": {
    "app/api/eps/**/*.ts": {
      "maxDuration": 60
    }
  }
}
